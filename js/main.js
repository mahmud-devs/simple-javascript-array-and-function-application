/* 

============= array and function =============

*/
let array = [
    3,
    5,
    6,
    7,
    11,
    77,
    82,
    8,
    26,
    37,
    "nam",
    "pojonon",
    "kojedviohuav",
    "oudbviebv",
];

for (i = 0; i < array.length; i++) {
    if ((array[i] === "nam", "pojonon", "kojedviohuav", "oudbviebv")) {
        array[i] = 50;
    } else {
    }
    console.log(`${i} X ${array[i]} = ${i * array[i]}`);
}

/*

    ================== function =================

*/

function namta(num1, num2) {
    let sum = num1 + num2;
    return sum;
}

namta(55, 98);

for (i = 0; i <= 10; i++) {
    console.log(`${i} X ${sum} = ${i*sum}`);
}
